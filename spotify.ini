[module/spotify]
type = custom/script
interval = 1
exec = ~/spotify-polybar/spotify.sh
tail = true
format-prefix = " "
format-prefix-foreground = ${color.green}

[module/spotify-control]
type = custom/script
interval = 1
exec = [[ "$(playerctl -p spotify status)" = "Playing" ]] && echo 0 || echo 1
click-left = playerctl -p spotify play-pause
